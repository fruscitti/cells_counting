---
phase: quick-260331-hi7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - releases/windows/cell_counter.spec
  - releases/windows/installer.nsi
  - .github/workflows/build-windows.yml
autonomous: true
requirements: []

must_haves:
  truths:
    - "Triggering workflow_dispatch on GitHub Actions launches a Windows build job"
    - "Build job produces CellCounterInstaller.exe as a downloadable artifact"
    - "Installed app opens from Start Menu with no console window"
    - "Uninstaller appears in Windows Add/Remove Programs"
  artifacts:
    - path: "releases/windows/cell_counter.spec"
      provides: "PyInstaller spec — onedir, no console, PySide6 collect_all, icon.png"
    - path: "releases/windows/installer.nsi"
      provides: "NSIS MUI2 script — Start Menu shortcut, uninstaller, registry entry"
    - path: ".github/workflows/build-windows.yml"
      provides: "CI workflow — windows-2022, workflow_dispatch, artifact upload"
  key_links:
    - from: ".github/workflows/build-windows.yml"
      to: "releases/windows/cell_counter.spec"
      via: "pyinstaller releases/windows/cell_counter.spec --distpath releases/dist"
    - from: ".github/workflows/build-windows.yml"
      to: "releases/windows/installer.nsi"
      via: "makensis.exe installer.nsi"
    - from: "releases/windows/installer.nsi"
      to: "releases/dist/CellCounter/"
      via: "File /r ${DIST_DIR}\\*.*"
---

<objective>
Add a GitHub Actions CI pipeline that builds a Windows NSIS installer (.exe) for the PySide6 desktop app on manual trigger.

Purpose: Non-technical users receive a professional installer they can double-click — no Python, no terminal.
Output: Three files: PyInstaller spec, NSIS script, GitHub Actions workflow. Running `workflow_dispatch` in GitHub produces `CellCounterInstaller.exe` as a downloadable artifact.
</objective>

<execution_context>
@/Users/ferar/fun/celulas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ferar/fun/celulas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ferar/fun/celulas/.planning/quick/260331-hi7-build-windows-installer-executable-for-c/260331-hi7-CONTEXT.md
@/Users/ferar/fun/celulas/.planning/quick/260331-hi7-build-windows-installer-executable-for-c/260331-hi7-RESEARCH.md

<interfaces>
<!-- Entry point and icon locations confirmed from codebase -->
<!-- Entry point: app.py (repo root) — NOT main.py (Gradio web version) -->
<!-- Icon: icon.png (repo root) — Pillow in build env enables auto PNG→ICO conversion -->
<!-- Dependencies: PySide6 6.11.0, opencv-python 4.13.0, numpy 2.4.3, pandas 3.0.1 -->
<!-- No requirements.txt / pyproject.toml — CI must install each package explicitly -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PyInstaller spec file</name>
  <files>releases/windows/cell_counter.spec</files>
  <action>
    Create the directory `releases/windows/` and write `cell_counter.spec`.

    Key requirements:
    - Use `collect_all('PySide6')` to collect datas, binaries, hiddenimports — this is the ONLY reliable way to include `platforms/qwindows.dll` (avoids Pitfall 1: qt.qpa.plugin crash)
    - hiddenimports also includes: `['cv2', 'numpy', 'pandas']`
    - Analysis pathex points to repo root: `['../..']`; script path: `['../../app.py']`
    - datas includes `('../../icon.png', '.')` to embed the icon
    - EXE: `console=False` (no terminal for end users), `icon='../../icon.png'` (Pillow auto-converts PNG→ICO), `name='CellCounter'`, `exclude_binaries=True`
    - COLLECT block (onedir, NOT onefile) — avoids Pitfall 4: slow 10-30s startup with PySide6
    - upx=True on EXE and COLLECT to reduce bundle size

    Spec runs from repo root via:
    `pyinstaller releases/windows/cell_counter.spec --distpath releases/dist --workpath releases/build`

    Use the exact spec structure from the research file (Architecture Patterns section).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('/Users/ferar/fun/celulas/releases/windows/cell_counter.spec').read()); print('spec syntax OK')"</automated>
  </verify>
  <done>
    `releases/windows/cell_counter.spec` exists and is valid Python. Contains `collect_all('PySide6')`, `console=False`, `exclude_binaries=True`, and a COLLECT block (onedir mode).
  </done>
</task>

<task type="auto">
  <name>Task 2: NSIS installer script</name>
  <files>releases/windows/installer.nsi</files>
  <action>
    Write `releases/windows/installer.nsi` using NSIS MUI2.

    Key requirements:
    - `!include "MUI2.nsh"` and `!include "LogicLib.nsh"`
    - Defines: APPNAME "Cell Counter", APPVERSION "1.0.0", PUBLISHER "Cell Counter Lab", INSTALL_DIR "$PROGRAMFILES64\CellCounter"
    - DIST_DIR points to `..\..\releases\dist\CellCounter` (output of PyInstaller COLLECT)
    - OutFile: `..\..\releases\CellCounterInstaller.exe`
    - `RequestExecutionLevel admin` — needed to write to Program Files and HKLM
    - MUI pages: WELCOME, DIRECTORY, INSTFILES, FINISH (install); CONFIRM, INSTFILES (uninstall)
    - `!insertmacro MUI_LANGUAGE "English"`
    - Install Section:
      - `SetOutPath "$INSTDIR"`
      - `File /r "${DIST_DIR}\*.*"` — copies entire onedir bundle
      - `CreateDirectory "$SMPROGRAMS\${APPNAME}"`
      - `CreateShortcut` for Start Menu: `$SMPROGRAMS\${APPNAME}\${APPNAME}.lnk` pointing to `$INSTDIR\CellCounter.exe`
      - `WriteUninstaller "$INSTDIR\Uninstall.exe"`
      - WriteRegStr to HKLM `Software\Microsoft\Windows\CurrentVersion\Uninstall\${APPNAME}` for DisplayName, UninstallString, Publisher
    - Uninstall Section:
      - `RMDir /r "$INSTDIR"`
      - Delete Start Menu shortcut and folder
      - `DeleteRegKey HKLM "Software\Microsoft\Windows\CurrentVersion\Uninstall\${APPNAME}"`

    Use the exact NSIS structure from the research file (Architecture Patterns section).
  </action>
  <verify>
    <automated>python -c "content=open('/Users/ferar/fun/celulas/releases/windows/installer.nsi').read(); checks=['MUI2.nsh','RequestExecutionLevel admin','PROGRAMFILES64','WriteUninstaller','DeleteRegKey']; [print(f'FOUND: {c}') or None for c in checks if c in content]; missing=[c for c in checks if c not in content]; print('ALL OK' if not missing else f'MISSING: {missing}')"</automated>
  </verify>
  <done>
    `releases/windows/installer.nsi` exists. Contains MUI2 includes, admin execution level, PROGRAMFILES64 install dir, Start Menu shortcut creation, WriteUninstaller call, and registry entries for Add/Remove Programs.
  </done>
</task>

<task type="auto">
  <name>Task 3: GitHub Actions workflow</name>
  <files>.github/workflows/build-windows.yml</files>
  <action>
    Create `.github/workflows/` directory and write `build-windows.yml`.

    Key requirements:
    - `on: workflow_dispatch` ONLY — no push/tag triggers (locked decision)
    - `runs-on: windows-2022` — NOT `windows-latest` (avoids Pitfall 2: windows-2025 has no NSIS)
    - Steps in order:
      1. `actions/checkout@v4`
      2. `astral-sh/setup-uv@v7` — uv is NOT pre-installed (avoids Pitfall 3)
      3. Install deps: `uv pip install --system pyinstaller pillow PySide6 opencv-python pandas numpy` — use `--system` flag (avoids Pitfall 6: pyinstaller not on PATH)
      4. PyInstaller build: `pyinstaller releases/windows/cell_counter.spec --distpath releases/dist --workpath releases/build`
      5. NSIS build (PowerShell): `cd releases/windows && & "C:\Program Files (x86)\NSIS\makensis.exe" installer.nsi`
         - Use `shell: pwsh` for the PowerShell `&` call operator
      6. Upload artifact: `actions/upload-artifact@v4`, name `CellCounterInstaller-${{ github.run_number }}`, path `releases/CellCounterInstaller.exe`, retention-days 30

    Use the exact workflow structure from the research file (Architecture Patterns section).
  </action>
  <verify>
    <automated>python -c "import yaml; wf=yaml.safe_load(open('/Users/ferar/fun/celulas/.github/workflows/build-windows.yml')); assert 'workflow_dispatch' in wf['on'], 'missing workflow_dispatch'; assert wf['jobs']['build']['runs-on']=='windows-2022', 'wrong runner'; print('workflow YAML valid, runner=windows-2022, trigger=workflow_dispatch')" 2>/dev/null || python3 -c "content=open('/Users/ferar/fun/celulas/.github/workflows/build-windows.yml').read(); checks=['workflow_dispatch','windows-2022','setup-uv','--system','makensis','upload-artifact']; missing=[c for c in checks if c not in content]; print('ALL OK' if not missing else f'MISSING: {missing}')"</automated>
  </verify>
  <done>
    `.github/workflows/build-windows.yml` exists. Uses `workflow_dispatch` trigger, `windows-2022` runner, `astral-sh/setup-uv@v7`, `--system` install flag, PowerShell `makensis.exe` step, and `actions/upload-artifact@v4` uploading `releases/CellCounterInstaller.exe`.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete, verify the full pipeline is wired:

1. Spec file references `app.py` (not `main.py`): `grep -n "app.py" releases/windows/cell_counter.spec`
2. NSIS DIST_DIR matches PyInstaller COLLECT name: both use `CellCounter`
3. Workflow artifact path matches NSIS OutFile: both reference `releases/CellCounterInstaller.exe`
4. No `--onefile` flag anywhere: `grep -r "onefile" releases/ .github/`
5. No `windows-latest` in workflow: `grep "windows-latest" .github/workflows/build-windows.yml` returns nothing
</verification>

<success_criteria>
- `releases/windows/cell_counter.spec` — valid PyInstaller spec, onedir, no console, collect_all PySide6
- `releases/windows/installer.nsi` — NSIS MUI2 script, admin elevation, Start Menu + uninstaller
- `.github/workflows/build-windows.yml` — workflow_dispatch, windows-2022, uv --system, artifact upload
- Triggering the workflow on GitHub Actions produces `CellCounterInstaller.exe` as a downloadable artifact with 30-day retention
</success_criteria>

<output>
After completion, create `.planning/quick/260331-hi7-build-windows-installer-executable-for-c/260331-hi7-SUMMARY.md`
</output>
